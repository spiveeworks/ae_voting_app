
contract ADT_Test =
  datatype adt = NoInts | OneInt(int) | TwoInts(int, int)
  type aliased('a) = ('a * 'a)
  record rec = {x : int, y : adt, z : aliased(int)}

  type state = option(int)

  entrypoint init() : option(int) = None

  entrypoint method(x : option(int), y : rec): adt = y.y
